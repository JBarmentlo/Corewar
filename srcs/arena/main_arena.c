#include "op.h"
#include "arena.h"

t_vm	init_vm()
{
	//verify input validity

	//malloc memory;
	//insert champions
	//create processes;
}

int		main(int ac, char **av)
{
	t_vm vm;

	vm = init_vm();
	while(there is a live process)
	{
		do_the_cycle(vm);
		display(vm);
	}
}