#include "arena.h"

t_arena	init_vm()
{
	//verify input validity

	//malloc memory;
	//insert champions
	//create processes;
}

int		main(int ac, char **av)
{
	t_arena vm;

	//parsing options
	vm = init_vm();
	/*
	while(No_winner)
	{
		do_the_cycle(vm);
		display(vm);
	}
	*/
}